velocity = float(input("Enter the change in velocity = "))

time = float(input("Enter the total time taken = "))

acceleration = velocity / time

print("The acceleration of the object is", acceleration)