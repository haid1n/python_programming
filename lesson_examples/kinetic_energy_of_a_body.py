mass = float(input("Enter the mass of the body = "))

velocity = float(input("Enter the velocity of the body = "))

kinetic = 1/2 * mass * velocity * velocity

print("The kinetic energy of the moving body =", kinetic)