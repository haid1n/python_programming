displacement = float(input("Enter the displacement of the object = "))

time = float(input("Enter the time taken by the object = "))

velocity = displacement / time

print("The velocity of the object is", velocity)